{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan cache:clear && php artisan migrate --force && php artisan session:table --force || true && php artisan migrate --force && php -S 0.0.0.0:$PORT -t public"
  }
}
